import ReactDOM from 'react-dom/client';
import SearchBar from '@/shortcutPanel/index';   // 你的搜索条组件
import './index.css'
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<SearchBar />);